#include <stdio.h>

void main() {

	for (int i = 0; i < 5; i++) {
		for (int j = 0; j < 5; j++) {
			for (int k = 0; k < 5; k++) {
				for(int l)
			}
		}
	}
	/*
	int N, M;
	int A[100];
	int C[100];

	int min_value = 99999;
	scanf_s("%d %d", &N, &M);

	for (int i = 0; i < N; i++) {
		scanf_s("%d", &A[i]);
	}

	for (int i = 0; i < N; i++) {
		scanf_s("%d", &C[i]);
	}

	for (int i = 0; i < N; i++) {
		if (min_value > C[i]) {
			min_value = C[i];
		}
	}*/
}